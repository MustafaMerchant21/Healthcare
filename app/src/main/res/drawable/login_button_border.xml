<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <stroke
        android:width="2dp"
        android:color="@color/colorPrimary" /> <!-- Adjust width and color as needed -->
    <corners android:radius="16dp" /> <!-- Optional: for rounded corners -->
    <solid android:color="@android:color/transparent" /> <!-- Optional: for a transparent background -->
</shape>